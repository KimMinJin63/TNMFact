import 'package:flutter/material.dart';

class MainNewsPage extends StatelessWidget {
  const MainNewsPage({super.key});
  static const route = '/mainNews';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}