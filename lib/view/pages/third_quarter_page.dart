import 'package:flutter/material.dart';

class ThirdQuarterPage extends StatelessWidget {
  const ThirdQuarterPage({super.key});
  static const route = '/thirdQuarter';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}