import 'package:flutter/material.dart';

class FirstQuarterPage extends StatelessWidget {
  const FirstQuarterPage({super.key});
  static const route = '/firstQuarter';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}